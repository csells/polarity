const {chromium}=require('playwright');(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage();p.on('console',m=>console.log('console',m.type(),m.text()));p.on('pageerror',e=>console.log('error',e.message));await p.goto('http://127.0.0.1:8787',{waitUntil:'domcontentloaded'});await p.waitForFunction(()=>window.polarity);console.log(await p.evaluate(()=>{const q=polarity;q.freeze();q.setKey('start',true);let err;try{q.advance(4194304)}catch(e){err=e.message}const c=q.core,e=q.emu,pc=c._emulator_get_PC(e);return{err,pc:pc.toString(16),sp:c._emulator_get_SP(e).toString(16),ticks:c._emulator_get_ticks_f64(e),bytes:Array.from({length:12},(_,i)=>q.read(pc-4+i))}}));await p.screenshot({path:'artifacts/debug.png',timeout:10000});await b.close()})().catch(e=>{console.error(e);process.exit(1)});
